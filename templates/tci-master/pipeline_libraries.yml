pipeline_libraries:
  tci-library:
    source:
      remote: https://github.com/TikalCI/tci-library.git
    defaultVersion: lts
    implicit: false
    allowVersionOverride: true
    includeInChangesets: true

